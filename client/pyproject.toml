[project]
name = "client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-iptables",
    "scapy",
    "netfilterqueue",
    "psutil",
    "pyroute2",
    "shared",
]

[tool.uv.sources]
shared = { workspace = true }
