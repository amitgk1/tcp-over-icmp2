[project]
name = "tcp-over-icmp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "netfilterqueue>=1.1.0",
    "python-iptables>=1.2.0",
    "scapy>=2.6.1",
]
